{% extends '::base.html.twig' %}

{% block content %}
    <div class="container">
        <p>
            <a href="{{ path('demontpx_user_edit', {username: user}) }}" class="btn btn-default">Modify user</a>
        </p>

        <dl class="dl-horizontal">
            <dt>Name</dt>
            <dd>{{ user }}</dd>
            <dt>Email</dt>
            <dd>{{ user.email }}</dd>
        </dl>
    </div>
{% endblock %}
